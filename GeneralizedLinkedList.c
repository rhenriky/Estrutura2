#include <stdio.h>
#include <stdlib.h>
#include "GeneralizedLinkedList.h"


int addAtom(Node **list, int atom){
    Node *no = (Node*)malloc(sizeof(Node));
    if(no == NULL) return -1;
    no->type = 0;
    no->
}

